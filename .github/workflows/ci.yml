name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # Matches the ">=3.12,<4.0" floor in pyproject.toml; older legs
        # fail at `poetry install` because the dependency tree resolves
        # only on 3.12+.
        python-version: ["3.12"]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install poetry
        run: pipx install poetry
      - name: Install dependencies
        run: poetry install --with dev
      - name: Lint (ruff)
        run: poetry run ruff check .
      - name: Smell budget (master plan v3.2 §3)
        run: poetry run python scripts/check_smells.py --target src
      - name: Type-check (mypy strict)
        run: poetry run mypy src tests
      - name: Test
        run: poetry run pytest --cov=protea_runners --cov-report=term --cov-fail-under=70
